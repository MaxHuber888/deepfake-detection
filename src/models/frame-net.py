from keras import layers, models, Model

# TODO: Make a model which classifies images within a video and keeps a
#  running average of labels, then checks final label against video target

# MODEL DEFINITION
# model = models.Sequential()
# model.add()